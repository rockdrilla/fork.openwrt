diff --git a/net/mac80211/tx.c b/net/mac80211/tx.c
index 7cad769b97..b82fcbfb31 100644
--- a/net/mac80211/tx.c
+++ b/net/mac80211/tx.c
@@ -1605,8 +1605,8 @@ int ieee80211_txq_setup_flows(struct ieee80211_local *local)
 		fq->memory_limit = 4 << 20; /* 4 Mbytes */
 
 	codel_params_init(&local->cparams);
-	local->cparams.interval = MS2TIME(100);
-	local->cparams.target = MS2TIME(20);
+	local->cparams.interval = MS2TIME(50);
+	local->cparams.target = MS2TIME(10);
 	local->cparams.ecn = true;
 
 	local->cvars = kvcalloc(fq->flows_cnt, sizeof(local->cvars[0]),
